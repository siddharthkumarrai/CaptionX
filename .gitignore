# Node / Frontend
node_modules/
dist/
.npm/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python / Backend
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
ENV/
env.bak/
venv.bak/

# Environment Variables
.env
.env.local
.env.*.local

# Adobe UXP Packaging
*.ccx

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Python environments (project venvs live outside version control)
.venv_py311/
.venv_real/
list/

# Test / build caches
.pytest_cache/

# Local run logs and generated reports (regenerated on every run)
*.log
tests/golden_last_report.txt

# Plugin build artifacts
plugin/build.log
plugin/.debug

# Duplicate media (canonical golden fixture lives in assets/ and is LFS-tracked)
backend/assets/
backend/storage/

# Binary audio fixtures are hosted outside git (see docs); only the canonical
# video fixture is versioned via Git LFS.
*.wav

# Accidental root-level npm stub (real lockfile is plugin/package-lock.json)
/package-lock.json
